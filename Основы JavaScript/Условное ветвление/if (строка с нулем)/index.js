'use strict';

if ("0") {
	alert( 'Привет' );
}

// ALERT выведется так как условие является непустой строкой, которое преобразуется в true
// если бы было число 0 или пустая строка, то не вывелось бы